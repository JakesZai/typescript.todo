var array = [
	"Pickup dry cleaning",
	"Clean batcave",
	"Save Gotham"
];

/*for (var index in array) {
	var value = array[index];
	console.log(`#${index}- ${value}`);
}*/

for (var value of array) {
	console.log(value);
}